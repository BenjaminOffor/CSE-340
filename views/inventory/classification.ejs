<% if (title) { %>
  <h1><%= title %></h1>
<% } else { %>
  <h1>Classification</h1>
<% } %>

<!-- Optional: Display flash messages if any -->
<% if (typeof messages === 'function') { %>
  <%= messages() %>
<% } %>

<!-- Display vehicle grid -->
<%- grid %>
